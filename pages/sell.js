import NewItem from '../components/NewItem';
import LoginNotice from '../components/LoginNotice';

const Sell = props => (
  <div>
    <LoginNotice>
      <NewItem />
    </LoginNotice>
  </div>
);

export default Sell;
